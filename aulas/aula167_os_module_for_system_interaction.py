# The os module for system interactions;
# "The 'os' module provides functions to interact with the operating system;
# For example, the os.path module provides functions for working with file paths, and the os.listdir() function can be used to list the files in a directory. The os.system() method allows you to execute operating system commands from within your Python code.

import os

os.system('cls')
os.system('echo "Hello World"')

# print('a' * 400)

